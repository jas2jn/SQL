The Data Definition Language (DDL) script in this repository is meant to instantiate several tables. If such tables already exist, they will be dropped and new instances will be created. 

The Data Manipulation Language (DML) script contains example queries including aggregate functions, intersections, and joins.